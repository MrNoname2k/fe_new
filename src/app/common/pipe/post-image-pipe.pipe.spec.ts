import { PostImagePipePipe } from './post-image-pipe.pipe';

describe('PostImagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PostImagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
