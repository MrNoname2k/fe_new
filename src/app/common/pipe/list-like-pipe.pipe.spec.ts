import { ListLikePipePipe } from './list-like-pipe.pipe';

describe('ListLikePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ListLikePipePipe();
    expect(pipe).toBeTruthy();
  });
});
