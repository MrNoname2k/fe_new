import { EnterEventDirective } from './enter-event.directive';

describe('EnterEventDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterEventDirective();
    expect(directive).toBeTruthy();
  });
});
