import { LikeEventDirective } from './like-event.directive';

describe('LikeEventDirective', () => {
  it('should create an instance', () => {
    const directive = new LikeEventDirective();
    expect(directive).toBeTruthy();
  });
});
